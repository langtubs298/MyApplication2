<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.fragment.StressWordFragment">

    <!-- TODO: Update blank fragment layout -->
    <ScrollView
        android:id="@+id/scrollWord"
        android:layout_width="match_parent"
        android:layout_height="@dimen/scroll_height">
        <TextView
            android:id="@+id/txtStressWord"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/margin_left"
            android:layout_marginTop="@dimen/margin_top"
            android:textSize="@dimen/text_size_pronunciation"
            android:textStyle="bold"
            android:textColor="@color/colorLightTeal"
            android:text="@string/stress_word_guide" />
    </ScrollView>


    <ListView
        android:layout_below="@id/scrollWord"
        android:id="@+id/listPracticeStressWord"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
    </ListView>

</RelativeLayout>
